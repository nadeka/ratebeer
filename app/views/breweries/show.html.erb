<p id="notice"><%= notice %></p>

<h1><%= @brewery.name %></h1>

<p>
  <i>Established in <%= @brewery.year %></i>
</p>

<h3><%= pluralize(@brewery.beers.count, 'beer', 'beers') %></h3>

<ul>
    <% @brewery.beers.each do |beer| %>
    <li>
        <%= link_to "#{beer.name}", beer %>
    </li>
    <% end %>
</ul>

<h3><%= pluralize(@brewery.ratings.count, 'rating', 'ratings') %>, average <%= @brewery.avg_rating %></h3>

<%= link_to('Edit', edit_brewery_path(@brewery)) if current_user %>
