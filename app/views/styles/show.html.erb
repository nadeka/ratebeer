<p id="notice"><%= notice %></p>

<h1><%= @style.name %></h1>

<p>
    <strong>Description</strong><br>
    <%= @style.description %>
</p>

<br>

<%= link_to 'Edit', @style if current_user %> <br>
<%= link_to('Destroy', @style, method: :delete, data: { confirm: 'Are you sure?' }) if current_user %>
